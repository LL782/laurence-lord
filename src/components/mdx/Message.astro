<div class="mdx-message tile"><slot /></div>

<style>
  .mdx-message {
    padding: var(--gap-md);
    margin: var(--gap-lg) 0;
    background-color: var(--pink-12);
    outline: var(--gap-sm) solid var(--pink-50);
    outline-offset: calc(var(--gap-sm) * -1);
  }
</style>
