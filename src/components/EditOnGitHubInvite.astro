---
import { siteConfig } from "../site-config";

interface Props {
  path: string;
}

const { path } = Astro.props;
const editUrl = `${siteConfig.githubRepo}/edit/main${path}`;
---

<p class="footer-edit">
  Spotted a typo?{" "}
  <a href={editUrl}>Edit on Github</a>
</p>
